 <Window 
    xmlns="https://github.com/avaloniaui" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:behaviors="clr-namespace:GitHubFolderDownloader.Behaviors" 
    xmlns:viewModels="clr-namespace:GitHubFolderDownloader.ViewModels" 
    xmlns:views="clr-namespace:GitHubFolderDownloader.Views" Title="{Binding Title}" Height="542" Width="720">
    <StackPanel>
        <StackPanel Grid.Row="1" Grid.RowSpan="1" Margin="5" Orientation="Horizontal">
            <Button Command="{Binding StartCommand}">
                <Image Source="resm:GitHubFolderDownloader.Images.run.png"/>
            </Button>
            <Button Command="{Binding StopCommand}">
                <Image Source="resm:GitHubFolderDownloader.Images.stop.png"/>
            </Button>
        </StackPanel>
        <views:Settings/>
        <views:Progress/>
        <TextBox AcceptsReturn="True" IsReadOnly="True" TextWrapping="Wrap" 
        VerticalAlignment="Stretch" Text="{Binding GuiModelData.Logs}" MinHeight="100" MaxHeight="200" Margin="2"/>
    </StackPanel>
</Window>